diff --git a/util/install.sh b/util/install.sh
index bf4492d..e9d1f69 100755
--- a/util/install.sh
+++ b/util/install.sh
@@ -5,6 +5,7 @@
 
 # Fail on error
 set -e
+set -x
 
 # Fail on unset var usage
 set -o nounset
@@ -46,8 +47,8 @@ if [ "$DIST" = "Ubuntu" ] || [ "$DIST" = "Debian" ]; then
         $install lsb-release
     fi
 fi
-test -e /etc/fedora-release && DIST="Fedora"
 test -e /etc/redhat-release && DIST="RedHatEnterpriseServer"
+test -e /etc/fedora-release && DIST="Fedora"
 if [ "$DIST" = "Fedora" -o "$DIST" = "RedHatEnterpriseServer" ]; then
     install='sudo yum -y install'
     remove='sudo yum -y erase'
@@ -55,7 +56,11 @@ if [ "$DIST" = "Fedora" -o "$DIST" = "RedHatEnterpriseServer" ]; then
     update='sudo yum'
     # Prereqs for this script
     if ! which lsb_release &> /dev/null; then
-        $install redhat-lsb-core
+        if [ "$DIST" = "Fedora" ]; then
+            $install lsb_release
+        else
+            $install redhat-lsb-core
+        fi
     fi
 fi
 test -e /etc/SuSE-release && DIST="SUSE Linux"
@@ -175,6 +180,7 @@ function mn_deps {
     else  # Debian/Ubuntu
         pf=pyflakes
         pep8=pep8
+        pylint=pylint
         # Starting around 20.04, installing pyflakes instead of pyflakes3
         # causes Python 2 to be installed, which is exactly NOT what we want.
         if [ "$DIST" = "Ubuntu" -a `expr $RELEASE '>=' 20.04` = "1" ]; then
@@ -187,10 +193,17 @@ function mn_deps {
                 pf=pyflakes3
                 pep8=python3-pep8
         fi
+        if [ "$DIST" = "Ubuntu" -a `expr $RELEASE '>=' 23.10` = "1" ]; then
+                pep8=pycodestyle
+        fi
+        if [ "$DIST" = "Ubuntu" -a `expr $RELEASE '<=' 20.04` = "1" ]; then
+                pylint=pylint3
+        fi
 
         $install gcc make socat psmisc xterm ssh iperf telnet \
-                 ethtool help2man $pf pylint $pep8 \
+                 ethtool help2man $pylint \
                  net-tools ${PYPKG}-tk
+        $install --no-install-recommends $pf $pep8
 
         # Install pip
         $install ${PYPKG}-pip || $install ${PYPKG}-pip-whl
@@ -211,7 +224,10 @@ function mn_deps {
 
     echo "Installing Mininet core"
     pushd $MININET_DIR/mininet
-    sudo PYTHON=${PYTHON} make install
+    sudo PYTHON=${PYTHON} make install-mnexec
+    sudo PYTHON=${PYTHON} make install-manpages
+    ${PYTHON} -m pip uninstall -y mininet || true
+    ${PYTHON} -m pip install -e . --no-binary :all:
     popd
 }
 
@@ -303,7 +319,7 @@ function install_wireshark {
     if ! which wireshark; then
         echo "Installing Wireshark"
         if [ "$DIST" = "Fedora" -o "$DIST" = "RedHatEnterpriseServer" ]; then
-            $install wireshark wireshark-gnome
+            $install wireshark
 		elif [ "$DIST" = "SUSE LINUX"  ]; then
 			$install wireshark
         else
